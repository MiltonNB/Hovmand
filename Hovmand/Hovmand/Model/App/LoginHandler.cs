namespace Hovmand.Model.App
{
    public class LoginHandler
    {
        private PasswordHash _pwHash = new PasswordHash();
        public bool CanLogin(string email, string password)
        {


            return false;
        }
    }
}